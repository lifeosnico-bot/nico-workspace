# How to Evaluate Stateful Agents: Starter Guide to Letta Evals

**Video ID:** MIHeLZk6Nlo
**URL:** https://www.youtube.com/watch?v=MIHeLZk6Nlo

---

## Transcript

Hey everybody, it is Cameron here at Leta and today I'm going to talk about Leta Eval. We released a tool called Leta Evals, which is a testing framework for stateful agents. Let eval allows you to test things like whether or not your agents use tools, whether or not they're getting the right answers, whether or not they're remembering things the way that they should. We built Leta Evals to make sure that you can go this kind of last mile to making your Leta agents production quality. A common flow that people have with Leta agents is that they will check what's called an agent file, which is a snapshot of an agent in a point of time and they'll commit it to a git repo. And a lot of our users wanted something that felt like continuous integration testing for agents. So if they opened a pull request on their repository that said like to change a a particular piece of a prompt or upgrade a model, they actually want to build a test into their GitHub action that will basically test whether or not that agent regressed in terms of performance. We also use it internally for all of our benchmarking and our leaderboard and things like that. And so we figured we would open source it so that people can actually build serious production scale Leta agents. If you are a person who likes experimenting with agents, this is actually an extremely good framework. Um, in some sense it it almost resembles a reinforcement learning framework. In this video, I'm going to give you an overview of the core concepts of how Leta Evol works to show you what an agent file looks like so that you can replicate it and test that agent file in many different environments. I'll show you how to do extremely simple evals to basically test whether or not the assistant said a particular word. Then I'm going to show you something called rubric grading. Rubrically I'm going to show you how you can do eval multi-turn conversation. Um and for letter agents multi-turn is very important because letter agents are capable of managing their memory, what they remember during a conversation. And in multi-turn conversations, you might want to verify that your agent actually remembered something that you told it. And I'll show you a quick example of how to, you know, kind of verify whether or not an agent did what you expected it to do. I will refer you to the repo. Um, there is a bunch of examples in there that should help make it very clear. If you go to docs.leta.com/evals, there will also be pretty comprehensive documentation about how to use Leta Evals. Um, there's so much more that you can do with Leta Evals and hopefully we'll do some more videos on this a little later. Let eval has a few core concepts that you have to understand in order to follow along with the video. So, I'm going to go over those now. Number one is a data set. And a data set is just a list of questions, essentially a list of messages to send to the agent. Your data set can optionally include things like a ground truth. For example, if you ask an agent what 2+ 2 is, you can provide a ground truth which is four. So you can use your data set to define what you expect the correct answer to be. You can also provide various things like rubric variables um and arguments to construct agents from functions, but those are more sophisticated and you should refer to the documentation on those. We'll just be covering the basic usage in this video. The second one is a target. A target is the agent that you are evaluating. Targets can be specified in three ways. One, they can be an agent file. When your target is an agent file, we'll look at every single observation in your data set. Replicate one agent per observation in that data set and then ask them the question that is in that observation. You can also provide an agent ID which is the identifier of your agent on your Letta server. This is a very different use case because that agent ID is actually modified in place. You're basically providing that agent with a script and you can actually use let eval in this way to almost train your agent by giving it many questions checking to see how well it did and you could even potentially give it feedback about how it did so that it learns how to do that benchmark better next time. Okay, so that's a target. You can also do agent factories which are functions but again that's a separate and more complicated example. Please refer to the docs. Number three is an extractor. An extractor is simply a function that takes all of the stuff that your agent did during its evaluation phase and it picks out some particular relevant piece so that you can grade it. This can be things like memory blocks to see what it remembered. It can be the actual assistant messages, which is kind of what it said. You can look at tool calls, tool returns, etc. There's plenty of different extractors. You can write your own custom functions, but but extractors are how you pull out the thing you want to grade. Number four is a greater. And graders can be toolbased, such as, does this thing contain the ground truth? For example, if I ask my agent, what is 2 plus two? and my ground truth in the observation is four, I can say, does the assistant response contain four? Then you would score that observation with a one. If there's no four, then it's a zero. That's very simple. That's toolbased grading. The other one is rubricbased grading where you actually use a second language model or a second letter agent to evaluate the responses of the first agent and a grader just accepts whatever the extractor gives it. Fifth, there is a gate. And a gate is a pass fail threshold that you specify for the entire evaluation. You can use this basically to throw an error when your evaluation fails. And this is really valuable if you are using leta evals inside of kind of a a deployment process to basically check whether or not your agent has gotten worse uh as a result of whatever modifications you've made to your code. So gates are extremely valuable for deploying evals at scale. And then of course number six there are results and this is how well your agents did. You can get metrics like average score, the standard deviation of scores, how many errors there are, all kinds of things. So the first thing I want to cover because it's uh you know helpful for understanding let eval is something called the agent file. We launched this file format called agent file um a little while ago. And an agent file you should think of as just a a snapshot of an agent. You just kind of like copy it, you dump it onto disk, and you can share it around. And then you can clone agents from that file all you want. Um I figured I would show you like kind of a very simple agent file. Under the hood, they're just JSON. Um so they're they're nothing too complicated. Um there's like names, you can add memory blocks, there's tools, you set up the language model configuration. So this is 40 mini. You specify how you want it to be embedded. Um just kind of every toggle. You can also have message history blah blah blah. So these are just kind of like little uh little snapshots of agents and you can create a new agent from this. Uh this one I've already pre-extracted. If you have an existing agent and you want to create an agent file, you can go to your dashboard, select an agent here. I'm going to go to Ezra, which is uh our team member who's a leta agent uh who handles support. You can talk to it on our Leta forum, or you can talk to it on Discord. Click the little three dots by the name, download agent file, and then you'll end up with an agent file that looks something like this. And here is an example of what Ezra looks like. You'll be able to see a ton of messages about what everything that Ezra has ever done. Okay, so agent files are a way of compressing an agent into um you know a a fixed text format. And agent files have this wonderful property which is that you can just check them into Git, right? They're almost like agents as code. Let evals was designed to work with agent files so that you can spin them up. you can replicate many agents um across your data set and evaluate them all at the same time using that uh agent file as a seed. Okay, so let's walk through a very simple example. I'll show you how to do kind of the basics of leta evals. In order to install it, it's super simple. You just do pip install leta evals. I already have it. Um you shouldn't be using pip. Pip is cursed. Um typically I would recommend using uv. um you should go follow the instructions about how to install UV so that you can do uh the correct uh uh package management for Python because Python uh was never intended to run uh the world and it does now and uh that was an error uh but UV kind of helps with that a little bit. So let's start with the simplest possible thing here which is our data set. Data sets can be JSONL, which is a list of JSON objects, or in this case, it is CSV. Go check out the documentation to learn about JSON L, but we'll stick with CSV right now because it's very easy. CSV, if you don't know, this is like a tabular data format. CSV stands for commaepparated value. It basically just means like it's an Excel sheet basically, but far simpler. In this case, it's a data set with two columns, input and ground truth. In this test, we're going to ask our agent three questions. We want our agents to answer the capitals of various countries. What is the capital of France? What is the capital of Japan? And what is the capital of Brazil? And then we have a second column here called ground truth. And a ground truth just says the answer is Paris, the answer is Tokyo, the answer is Brazilia. Ideally, we would get 100% correct. In this case, our agents should be able to guess Paris, Tokyo, and Brazilia. Okay, so that's our data set. Next, we are going to define our eval. The way you define how you want your eval to work is using what's called a suite file. So, au.yamel file in this case. You give it a name. You can describe it if you want. a simple single turn Q&A eval using the last assistant extractor and tool grader. And we'll show you what that means in just a second. We're going to tell Leta eval where to find our data set. So in this case, it is in the same directory. It's just data set.csv. Okay. Then we have our target. This is how we specify the agent that we're evaluating. We're going to say leta agent. Um our kind here is a leta agent. We will be testing a Leta agent here. And then we're going to specify the location of the agent file here. It's going to be that agent file that I just showed you, which is this kind of QA agent. And then you can specify the base URL for your Leta server. I'm going to use Leta cloud here, but you can also use your self-hosted server if you would like. So let's talk about graders. I want to start from the inside and work my way up. The extractor here is going to look inside of the agent's response and it's going to take whatever the last assistant message is. When you ask a letter agent something, it can do a bunch of stuff. It can do like a ton of assistant messages and tool calls and it can potentially go for a very very very long time. This extractor will go find the most recent assistant message that the agent produced. Then we're going to use our grading tool that is kind is tool function contains. What that means is we're going to look at the last assistant message. We're going to see if the assistant message contains the ground truth answer. What that means is we're going to ask our agent what's the capital of France and then it will say certainly the the capital of par France is blah blah blah blah blah. And hopefully the word Paris is inside of the assistant's response. If that if Paris appears in assistant response, then we assign a score of one. If not, we assign a score of zero. And then we're going to assign that score to something called contains check. You can name that whatever you want. Uh those are just names for you to remember. And then we're going to put that in the greater section. You can also do any other uh metric in here by just specifying its name. You can have an arbitrary number of metrics, but check out the docs for that. Lastly, we are going to define our gate. We're going to look at the contains check score. We're going to check if that score is greater than or equal to 67. If it's greater than 67, this is this will not error out. Otherwise, if it's less than that, we're going to throw an error and we can catch that however we want. Um, and just say like, "Oh crap, we can't deploy these agents. Like, they're really bad at identifying uh the capital of Paris. Oh my god, the capital of France. Oh man, I don't even know. I don't honestly I don't know what goes on over there. I just So, this is our suite. This specifies the let eval. Running this evaluite is very easy. You just type let evals run and then you give it the suite file that you want to execute like this and you'll start getting this kind of live display of what's going on. We can see that we're sending all the prompts to each of our three agents. And then here we got our results. We see we had three total samples. We had three attempts. The average score was 67%. And we can see here that this agent uh sample three did not perform well. It did not have the correct ground truth inside of it. You'll notice here that we don't actually see the result. All we see that was that ground truth was false. But we don't know exactly what it was that this agent wrote. If you want to save the results that your sample agents actually generated, you should use d-output and then the name of the folder you want to save it to. So here I'm going to do dash dash output and then put it in this results directory. Now we're going to start saving information inside of the results directory. We can see that again sample 3 failed. And if you remember sample 3 I believe was about the capital of uh Brazil. So let's go look and we can see that we have this results folder here. header just contains the you know kind of the the information that's essentially in the suite file so that you can go look back and see the configuration that this eval was run with. You can look at the re results here. If we look at the actual assistant response, it used the special accent I which is actually the correct spelling of the capital. But in our ground truth in the data set, we actually did not use the special I. And so that's why this thing is failing. So we can actually So we can actually correct this I believe. Let's try this again. And boom, we got it right because now the accent is correct inside of our data set. And then of course there is a summary.json uh file that we'll get out which has various metrics about uh just kind of overall information. All right. So let's talk about rubric grading. Rubric grading is extremely powerful. The previous toolbased grading system that we did doesn't really scale to pretty complicated like qualitative or um multiaceted uh output from your agent that can be difficult to evaluate. Rubric rating will take the agent's output and give it to another agent to evaluate. In this data set, my input is explain why Paris is the capital of France in two to three sentences. Right? So this is words, right? It's it's this is a qualitative thing thing because there is no definitive answer to why right you can answer this question in many different ways. And then there's a ground truth here that something like Paris has been the capital of France for centuries and it's the capital the country's political, economic and cultural center. Right? So this is our best definition of a ground truth. Okay? So keep that in mind. We're trying to get qualitative explanations from our agent. So here we're actually just going to give our agent kind of a overview of how to judge the target agents responses. So we're going to ask it for accuracy, which is 50% of the score, completeness, which is 30% of the score, and length, which is 20% of the score. Um, and then just some rough guidelines about what the score should look like. You can put whatever you want in here. As long as you can articulate to your judge something that should map to a score between zero and one, you can grade it. Okay, so that's our rubric. Now, let's go to check out the suite that we used to define it. Everything else here is pretty simple. It's more or less what we saw before. Our target is going to be the exact same Q agent QA agent from before. Let's focus here on the graders component because that's the only thing that really differs in this case. We're going to use a letter judge as the greater kind. This is the prompt that we're going to give it rubric.txt and then the extractor is last assistant which again means we're going to go into the agent that we're evaluating. Look at its most recent assistant message. send that to the judge and the judge will have the rubric and the last assistant response and it will basically say okay this agent did 7 it could have done better in this way and then we have a gate on this quality score so that we require at least 70% accuracy so let's go ahead and run this and I'm going to save the output again so we can go look at it in a second Again, we're going to see this kind of a live feed as we go along. Now, both of them are in the grading phase. We can see our quality score. So, the first one got 100%. The second one got 85%. And then the letter agent that's judging also has to provide a rationale where they say the submission demonstrates strong performance across all evaluation criteria. The main reason it doesn't receive a perfect one is that while the ground truth emphasizes Tokyo's functional role as the political, economic, and cultural hub, the agent's response places greater emphasis on the historical context rather than explaining the present-day functional reasons why Tokyo serves as a c as a capital. So you can get really detailed judging by using language models as the judge for a particular output. So rubric rating is extremely powerful. And of course, because this is running on a little server, you can actually go look in your ADE to see what their output was. So here we have the QA agents. These are the two that we spun up, one for each observation, one for France, one for Japan. And then we have the judge for each of those agents. For example, this agent is the simple QA agent. And here is its answer. Right? This is the thing that Leta Eval sent. And then this is the thing that it responded with. This gives you so much power. Anything that you can describe how to grade can be graded. This means things like writings. It means proofs. It means code style. Anything you can possibly imagine. If you can write it in a rubric, it can be scored and graded. And that means you can write pretty rigorous tests to determine whether or not your agent is doing what you think it should be doing. So the rubric is extremely powerful. So that's rubric rating. You can go way farther with this. You can use base language models if you want, but um or you can just go through letter agents, which we think is great. Honestly, uh if you want to use OpenAI as a judge, that's fine with me. You can be uncool if you want to. And then let me show you kind of one of the last takeaways here. Multi-turn conversation. This data set specification here is using JSON L which stands for JSON lines which means that each row contains one observation that you will send to a target. Note here that we have input and it's a list. The first message is remember that my favorite European capital is Paris. We'll send that to the agent. We'll wait for them to do whatever it is they're going to do and then when they're done we say what's my favorite European capital. Then that will complete the agent's response. Then we're going to look for a ground truth of Paris and then a ground truth in Tokyo. Okay, nothing complicated, very very simple. As soon as you put something inside of a list, you are doing multi-turn evaluation. You will send that agent multiple questions. You will send that agent multiple prompts in sequence. You can also do this inside of the CSV format as well if you would like to. I've made a super quick agent that has the ability to remember things and it has a tool so it can add things to its memory blocks and then a single memory block that is called user preferences that's currently empty. So hopefully I'll tell this agent information about what my favorite city is and then it will store this information inside of its user preferences block and then we'll build an evaluation suite to test to see whether or not it remembered the thing that I was hoping it would remember. So now let's take a look at our suite. Now we're just using our data set as JSON data set.jsonl. We are using leta agent again but this time with our new agent file. Then we're going to look at our greater here. We're going to have a metric here called memory check. And all this is going to do is say does the block label does the memory block user preferences contain the ground truth which is Paris or Tokyo depending on which observation you're on. This extractor is built in. It'll basically say go get the user preferences memory block. Pass that to the contains function. If that memory block contains the word Paris or whatever the ground truth is, then it gets a one. Otherwise, it gets a zero. And then we're going to require that all observations succeed, get a one. Let's go ahead and run this and see what we get. Okay, so we're sending each uh observation here. You can see two out of two are being evaluated by the agent and then we get our results here and all of the agents have succeeded. So let's go take a look at those agents in the AD because they're still on the server. We can actually go examine them if we want to. So let's just pick one of these at random. You can see here this is the first message that lea eval said remember that my favorite Asian capital is Tokyo and then it did a memory replace operation and then it added my favorite Asian capital is Tokyo. So it stored that information and remembered it. Then I said what's my favorite Asian capital and then it said your favorite Asian capital is Tokyo. And we can see that it was stored inside of the user preferences block. So when we're doing the extractor, we're actually going to take the value out of this, pass it to the contains function, and see if the word Tokyo is inside of this memory block. So that's super super simple. There are many many many other ways to grade and extract and define judges and all of that stuff. It's really cool. It's extremely flexible. You can do a lot of evals at scale. you know, we do this for, you know, thousands of different observations. Our customers do this at scale to build production ready agents. And I think there's also a lot of very cool things you can do that are very outside of the box. For example, if you pass an agent ID as your target, you are repeatedly calling the same agent. And one thing we know about let agents is they actually learn, right? The more you talk to them, the better they get. So you can actually repeatedly evaluate them on the same benchmark as a form of training. You can do something even more interesting, which is you can actually build customized teacher agents. These are rubric grading Leta agents that are specialized at judging a particular task because they have judged many, many, many, many tasks. They've got a lot of messages. They have a lot of experience. Maybe you've worked with them about all of the little intricacies of judging like writing styles for example. And then you can have a longived writing agent be taught by a long lived editing agent and both of them can get better at the task because they're constantly checking each other's work and communicating. That's like kind of one idea I have in mind. There's so much more you can do here. I hope you play around with this and kind of get a good sense of how let it eval works and maybe how you can put it inside of your safe agent workflow. I'll put this code up in a separate repository so people can play with it. If you want to know more examples, I recommend that you check out the examples directory inside of the letter eval repo. It it covers kind of everything you might want to use it for. I hope you have a lovely morning, lovely evening, a lovely Tuesday. whatever time is completely meaningless. And uh thanks for watching. [music]
